import React from 'react';
import './Preamble.scss';

const Preamble: React.FC = () => (
  <div className="Preamble" data-testid="Preamble">
    Preamble Component
  </div>
);

export default Preamble;
